<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.example"
      android:versionCode="1"
      android:versionName="1.0">
      <!-- asdf -->
      <!-- more more more changes -->
    <application android:label="AnimationDemo" android:icon="@drawable/icon" android:debuggable="true">
        <activity android:name="MyActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- I'm making MORE changes -->
        <activity android:name=".ListAnimation"/>
        <activity android:name=".FormAnimation"/>
        <activity android:name=".ActivityTransition"/>
        <activity android:name=".ViewAnimation"/>
        <activity android:name=".ViewAnimationClipped"/>
        <!-- More changes -->
        <activity android:name=".ViewScaleExample"/>
        <activity android:name=".MultipleMixedAnimations"/>
        <activity android:name=".FrameAnimation"/>
        <activity android:name=".FormLayoutAnimation"/>
    </application>
</manifest> 
